let index = {

	init: function() {
		$("#btn-save").on("click", () => {
			this.save();
		});
	},

	save: function() {
		
		let data = {
			title : $("#Title").val(),
			content : $("#summernote").val(),
		};
			console.log("잘들어감?" + data.title);
			console.log("잘들어감?" + data.content);
		
	/*	$.ajax({
			type: "post",
			url: "/auth/joinProc",
			data: JSON.stringify(data), 
			contentType: "application/json; charset=utf-8",
			dataType: "json",  
		}).done(function(resp) { 
			alert("회원가입이 완료되었습니다.");
			location.href = "/";
		}).fail(function(error) {
			alert("회원가입 실패하였습니다.");
		});*/
		
	},
}

index.init();