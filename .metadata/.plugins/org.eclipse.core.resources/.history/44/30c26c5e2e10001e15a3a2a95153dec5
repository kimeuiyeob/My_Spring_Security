let index = {

	init: function() {
		$("#btn-save").on("click", () => {
			this.save();
		});
	},

	save: function() {
		
		let data = {
			username: $("#Title").val(),
			password: $("#summernote").val(),
		};
			console.log("잘들어감?" + data.username);
			console.log("잘들어감?" + data.password);
		
	/*	$.ajax({
			type: "post",
			url: "/auth/joinProc",
			data: JSON.stringify(data), 
			contentType: "application/json; charset=utf-8",
			dataType: "json",  
		}).done(function(resp) { 
			alert("회원가입이 완료되었습니다.");
			location.href = "/";
		}).fail(function(error) {
			alert("회원가입 실패하였습니다.");
		});*/
		
	},
}

index.init();